### Tasks description for Lesson 5:

####  омментарии:

«адани€ к уроку 5 выполнены.

**ѕримечание к заданию 1:**

я не перегрузил %, потому что не понимаю, что такое остаток от делени€ дл€ рациональных чисел.

**ѕримечание к заданию 2:**

¬ класс *Complex* добавлено еще несколько операций: 

 - деление
 - поддержка тригонометрической формы (пол€рные координаты), причем соответствующие величины реализованы как properties, вычисл€емые "на лету".
 - через тригонометрическую форму реализовано возведение в степень - *Pow()* - формула ћуавра. » сразу вытекающие из него *Sqrt()*, *Sqr()*, *Root()* - корень n-степени.
 - также реализована экспонента - *Exp()*.

я подумывал сделать еще *Sin()* и *Cos()* (т.к. через *Exp()* это сделать довольно просто), но решил не увлекатьс€ и не уходить от задани€.

» также € не стал переусложн€ть перегрузку *ToString()*, обрабатыва€ всевозможные специальные случаи, оставив простой вывод интерполированной строки.
